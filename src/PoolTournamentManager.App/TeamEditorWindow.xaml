<Window x:Class="PoolTournamentManager.App.TeamEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Icon="Assets/Logo.png"
        Background="{DynamicResource AppBackgroundBrush}">
    <StackPanel Margin="16">
        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="16" Margin="0,0,0,12" />

        <TextBlock Text="Team Name" />
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

        <TextBlock Text="Division" />
        <TextBox Text="{Binding Division, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

        <TextBlock Text="Location (pool hall)" />
        <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,2,0,6"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NonEmptyToVisibilityConverter}}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Click="Save_Click" IsDefault="True" MinWidth="80" Padding="8,4" Margin="0,0,8,0" />
            <Button Content="Cancel" Click="Cancel_Click" IsCancel="True" MinWidth="80" Padding="8,4" />
        </StackPanel>
    </StackPanel>
</Window>
