# Progress

Tracks what's changed and what's next during development. Newest entries at
the top of each section.

## Current status

Planning stage — no application code written yet. Architecture plan drafted
covering project structure, domain model, tournament-format algorithms, the
admin/display window live-sync mechanism, persistence, and a phased
`0.1`–`0.8` build sequence (see plan history / CLAUDE.md versioning policy).

## Next steps

- [ ] 0.1 — Player roster CRUD: `Player` entity, first EF Core migration,
      admin roster grid (add/edit/deactivate, Fargo/USAPL/TAP/APA rating
      fields + contact info).

## Change log

<!--
Entry format:

## v0.X[.Y] — YYYY-MM-DD

- What changed.
- Why (if not obvious).
-->
